<UserControl x:Class="Logi_Aide.UCContact"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Logi_Aide"
             mc:Ignorable="d" 
             d:DesignHeight="50" Width="auto">
    <Grid Background="Beige" MinWidth="150">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Image Grid.Column="0" Source="{Binding Image}" Width="20"/>
        <TextBlock Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Top" Text="{Binding Nom}" FontSize="16" Margin="5,0,5,0" TextWrapping="Wrap"/>
        <TextBlock Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Bottom" Text="{Binding Prenom}" FontSize="16" Margin="5,0,5,0" TextWrapping="Wrap"/>
        <TextBlock Grid.Column="2" x:Name="Ordreprio" TextAlignment="Center" Text="{Binding OrdePrio}"  HorizontalAlignment="Right" Background="#FFB4B1B0" Width="20" FontSize="30" FontWeight="Bold" Foreground="White"/>
    </Grid>
</UserControl>
